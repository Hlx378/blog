# liusep.cn 操作手册

> 想改什么，就在这里找。左边是「我想做什么」，右边是「改哪个文件」。

---

## 0. 最常用的三条命令

打开终端，先进入项目目录：

```bash
cd /d/liuzi/blog
```

| 命令 | 作用 | 什么时候用 |
|---|---|---|
| `npx hexo server` | 本地预览，浏览器开 http://localhost:4000 | **写完文章先本地看效果** |
| `npx hexo clean` | 清掉缓存和构建产物 | 改样式不生效时 |
| `git push` | 推送上线 | 改完确认没问题后 |

> ⚠️ 你的电脑上 bash 工具不可用（缺 coreutils）。**如果命令报错，改用 PowerShell 执行。**

---

## 1. 写一篇新文章 ⭐ 最常用

### 方式一：命令行创建（推荐）

```bash
npx hexo new "文章标题"
```

会在 `source/_posts/` 下生成一个 `.md` 文件。

### 方式二：手动创建

直接在 `source/_posts/` 里新建 `.md` 文件，文件名建议用英文（会成为网址的一部分）。

### 文章模板（复制这个改）

```markdown
---
title: 文章标题
date: 2026-09-24 10:00:00
updated: 2026-09-24 10:00:00
categories:
  - 分类名
tags:
  - 标签1
  - 标签2
description: 一句话摘要，会显示在首页卡片上
cover: /img/bg-star.jpg
top_img: /img/bg-star.jpg
---

正文从这里开始，用 Markdown 写。

## 二级标题

普通段落。

- 列表项
- 列表项

```代码块
console.log("hello")
```

> 引用样式
```

### front matter 字段说明

| 字段 | 必填 | 说明 |
|---|---|---|
| `title` | ✅ | 文章标题 |
| `date` | ✅ | 发布日期，格式 `YYYY-MM-DD HH:mm:ss` |
| `categories` | 建议 | 分类，**会出现在「分类」页面** |
| `tags` | 建议 | 标签，**会出现在「标签」页面** |
| `description` | 建议 | 摘要，显示在首页卡片 |
| `cover` | 可选 | 卡片封面图，不填用默认图 |
| `top_img` | 可选 | 文章页顶部大图 |
| `comments` | 可选 | 设 `false` 关闭该篇评论 |
| `sticky` | 可选 | 数字，置顶（数字越大越靠前） |

> 💡 **关于分类和标签**：不用手动维护索引页！Hexo 会自动汇总。
> 你只要在文章里写 `categories` 和 `tags`，`/categories/` 和 `/tags/` 页面就会自动更新。

---

## 2. 改网站基本信息

**文件：`_config.yml`**（项目根目录）

```yaml
title: liusep                      # 网站标题
subtitle: '在技术上不断进步'        # 副标题
description: '记录学习，督促成长'    # 网站描述（SEO 用）
keywords: 'liusep,学习笔记'         # 关键词（SEO 用）
author: liusep                     # 作者名
language: zh-CN                    # 语言
timezone: 'Asia/Shanghai'          # 时区
url: https://liusep.cn             # 网站地址（改了会影响所有站内链接！）
```

**其他常改的：**

```yaml
index_generator:
  per_page: 10          # 首页每页显示几篇文章

permalink: :year/:month/:day/:title/    # 文章网址格式
```

---

## 3. 改外观（主题设置）

**文件：`_config.butterfly.yml`**（项目根目录，26 KB，有注释）

### 常用位置速查

> 下面的行号是**实测**的（文件共约 1133 行），会随你编辑而移动，**优先用 `Ctrl+F` 搜关键词**，行号只作参考。

| 想改什么 | 搜这个关键词 | 实测行号 |
|---|---|---|
| **导航栏菜单** | `menu:` | 20 |
| **社交链接**（GitHub/邮箱） | `social:` | 53 |
| **头像** | `avatar:` | 63 |
| **背景大图** | `index_img:` | 74 |
| **打字机副标题** | `subtitle:` | 156 |
| **首页文章布局** | `index_layout:` | 185 |
| **文章版权声明** | `post_copyright:` | 210 |
| **页脚版权** | `footer:` | 266 |
| **侧边栏卡片** | `aside:` | 281 |
| **公告卡片** | `card_announcement:`（在 `aside:` 下面，缩进两格） | 301 |
| **暗色模式** | `darkmode:` | 390 |
| **搜索** | `search:` | 481 |
| **分享按钮** | `share:` | 519 |
| **评论系统** | `comments:` | 538 |
| **默认明/暗** | `display_mode:` | 819 |
| **背景特效**（流星/彩带） | `canvas_ribbon:` | 864 |
| **烟花特效** | `fireworks:` | 894 |
| **图片灯箱** | `lightbox:` | 923 |
| **自定义样式注入** | `inject:` | 1065 |

### 举几个例子

**改导航栏菜单**（第 20 行附近）：

```yaml
menu:
  首页: / || fas fa-home
  归档: /archives/ || fas fa-box-archive
  分类: /categories/ || fas fa-folder-open
  标签: /tags/ || fas fa-tags
  关于: /about/ || fas fa-user
  # 加新的一项：
  作品集: /projects/ || fas fa-cube
```

格式是：`显示名: 链接 || 图标类名`

**改打字机文字**（第 156 行附近）：

```yaml
subtitle:
  enable: true
  effect: true
  sub:
    - 记录学习 · 督促成长
    - 每一步都算数
    - 保持好奇，持续更新      # 加一行就多一句轮播
```

**改侧边栏公告**（第 301 行附近，注意它**缩进在 `aside:` 里面**）：

```yaml
aside:
  # ... 上面还有 card_author / card_webinfo 等
  card_announcement:
    enable: true
    content: 这里写你想说的话
  # ... 下面还有 card_recent_post 等
```

> ⚠️ 这一项前面有**两个空格**的缩进，是 `aside:` 的子项。如果直接写在文件最左边顶格，YAML 会报错。

---

## 4. 改样式 / 加动效 ⭐ 后面做流星动效看这里

**文件：`source/css/custom.css`**

这个文件是通过 `inject` 注入的，**不修改主题源文件**，所以主题升级不会丢你的改动。

现在里面已有：
- 横幅背景优化（`cover` + 定位）
- 暗色卡片半透明 + 背景模糊
- 头像微光 + 悬停旋转
- 移动端适配

**加自定义 CSS 的写法**：

```css
/* 改首页大标题 */
#site-title {
  font-size: 3.5rem;
  letter-spacing: 0.1em;
}

/* 改卡片圆角 */
#recent-posts > .recent-post-item {
  border-radius: 16px;
}
```

**加自定义 JS**（流星动效会用到）：

1. 新建 `source/js/meteor.js`
2. 在 `_config.butterfly.yml` 的 `inject.bottom` 里注册：

```yaml
inject:
  head:
    - <link rel="stylesheet" href="/css/custom.css?v=1">
  bottom:
    - <script src="/js/meteor.js"></script>
```

> ⚠️ **改了 CSS/JS 一定要 `npx hexo clean` 再重新构建**，否则可能加载旧缓存。

---

## 5. 改图片资源

**目录：`source/img/`**

| 文件 | 用途 | 建议尺寸 |
|---|---|---|
| `avatar.jpg` | 头像 | 400×400 以上，正方形 |
| `bg-star.jpg` | 背景大图 | **≥2560×1440**（现在只有 799×720，偏小会糊） |

**站点图标：`source/icon/`**

| 文件 | 用途 |
|---|---|
| `favicon.ico` | 浏览器标签页图标（主用） |
| `favicon.png` | 64×64 |
| `favicon-32.png` | 32×32 |
| `favicon-16.png` | 16×16 |
| `favicon.svg` | 矢量版 |

> ⚠️ **重要**：站点图标必须放在 `source/icon/`，**不能放 `source/img/`**。
> 因为主题自带 `source/img/favicon.ico`，会覆盖同名文件。

**换背景图后要改的地方**（`_config.butterfly.yml`）：

```yaml
default_top_img: /img/bg-star.jpg
index_img: /img/bg-star.jpg
archive_img: /img/bg-star.jpg
tag_img: /img/bg-star.jpg
category_img: /img/bg-star.jpg
cover:
  default_cover:
    - /img/bg-star.jpg
```

---

## 6. 改独立页面

**目录：`source/<页面名>/index.md`**

现在有：

| 路径 | 文件 | 说明 |
|---|---|---|
| `/about/` | `source/about/index.md` | 关于我 |
| `/categories/` | `source/categories/index.md` | 分类页（`type: categories`） |
| `/tags/` | `source/tags/index.md` | 标签页（`type: tags`） |

**新建一个页面的方法**：

```bash
npx hexo new page "projects"
```

会生成 `source/projects/index.md`，内容模板：

```markdown
---
title: 作品集
date: 2026-09-24 10:00:00
type: page          # 或用 about / categories / tags 等特殊类型
top_img: /img/bg-star.jpg
comments: false
---

这里写页面内容，支持 Markdown 和 HTML。
```

然后去 `_config.butterfly.yml` 的 `menu` 里加导航入口。

> 💡 **归档页 `/archives/` 不用手动建**，Hexo 自动生成。

---

## 7. 写 Markdown 常用语法

```markdown
# 一级标题（文章里一般不用，标题已由 front matter 的 title 提供）
## 二级标题
### 三级标题

**加粗**  *斜体*  `行内代码`  [链接](https://example.com)

- 无序列表
- 第二项

1. 有序列表
2. 第二项

> 引用文字

| 表头 | 表头 |
|---|---|
| 内容 | 内容 |

---（分隔线）
```

**Butterfly 特有标签**（更美观）：

```markdown
<!-- 提示框 -->
{% note info %}
这是一条提示
{% endnote %}

<!-- 标签页 -->
{% tabs %}
<!-- tab 标签一 -->
内容一
<!-- endtab -->
<!-- tab 标签二 -->
内容二
<!-- endtab -->
{% endtabs %}
```

**插入图片**：

```markdown
![图片说明](/img/你的图片.jpg)
```

---

## 8. 发布上线流程

```bash
cd /d/liuzi/blog

# 1. 本地预览确认
npx hexo server
# 浏览器打开 http://localhost:4000 检查

# 2. 确认没问题后推送
git add -A
git commit -m "新增文章：xxx"
git push
```

推送后 Vercel 会**自动构建并部署**，等 1–2 分钟即可看到线上更新。

> ⚠️ **推送后请验证是否真的成功**：
> ```bash
> git fetch origin
> git rev-parse HEAD        # 本地
> git rev-parse origin/main # 远程
> ```
> 两个值相同才算推送成功。

---

## 9. 出问题怎么排查

| 症状 | 先试这个 |
|---|---|
| 改了样式没生效 | `npx hexo clean` 后重新构建；浏览器 `Ctrl+Shift+R` 强刷 |
| 某个页面 404 | 检查 `source/<页面名>/index.md` 是否存在，front matter 的 `type` 对不对 |
| 站内链接跳到错误域名 | 检查 `_config.yml` 的 `url` |
| 图片不显示 | 检查路径是否以 `/img/` 开头，文件是否真在 `source/img/` |
| 构建报错 | 看报错里的文件名和行号，多半是 YAML 缩进问题（**不能用 Tab，只能空格**） |
| 线上没更新 | 看 Vercel 的 Deployments 页面，检查构建是否成功 |

**YAML 格式铁律**：
- 缩进只能用**空格**，不能用 Tab
- 冒号后面要有**一个空格**：`title: 我的网站`（不能写成 `title:我的网站`）
- 含特殊字符的值要用引号包起来，如 `subtitle: '在技术上不断进步'`

---

## 10. 文件对照总表（速查）

| 我想改… | 去这个文件 |
|---|---|
| 网站标题/作者/域名 | `_config.yml` |
| 导航栏、头像、背景、暗色、搜索、评论 | `_config.butterfly.yml` |
| 写文章 | `source/_posts/xxx.md` |
| 关于页 | `source/about/index.md` |
| 改配色、加动效 | `source/css/custom.css` |
| 加 JS 动效 | `source/js/xxx.js` + 在 `_config.butterfly.yml` 的 `inject` 注册 |
| 换头像 | `source/img/avatar.jpg` |
| 换背景图 | `source/img/bg-star.jpg` |
| 换站点图标 | `source/icon/favicon.*` |
| 部署配置 | `vercel.json` |
| 依赖版本 | `package.json` |

---

## 11. 暂时不用碰的文件

| 文件 | 为什么不用碰 |
|---|---|
| `_config.landscape.yml` | 另一个主题的配置，你没用它 |
| `themes/` | 空的，主题走 npm 装在 `node_modules` |
| `node_modules/` | 依赖目录，不要手动改 |
| `.github/dependabot.yml` | 依赖自动升级机器人，可选调整频率 |
| `package-lock.json` | 自动生成，别手动改 |
| `db.json` | Hexo 运行缓存，已在 .gitignore 里 |
| `scaffolds/` | 文章模板，想改新建文章的默认格式可以动 |

---

## 12. 建议的维护节奏

| 频率 | 做什么 |
|---|---|
| 每次写文章 | `npx hexo server` 本地看一遍再推 |
| 每周 | 看 Vercel 部署是否正常 |
| 每月 | 检查背景图/头像等资源是否要更新 |
| 偶尔 | 看 Dependabot 的依赖升级 PR（**Hexo 大版本先别升**） |

---

*最后更新：2026-09-23*
